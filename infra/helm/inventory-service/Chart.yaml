apiVersion: v2
name: inventory-service
description: Helm chart for Inventory Service microservice
type: application
version: 0.1.0     # Chart version
appVersion: "1.0"  # App version (can match Docker tag)
