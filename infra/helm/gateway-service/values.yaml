replicaCount: 1

image:
  repository: ku4marez/myrepo
  tag: gateway-latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 7070

ingress:
  path: /gateway

resources: {}

env:
  SPRING_PROFILES_ACTIVE: k8s
